Currently just a backup for dotfiles. Customization (particularly on bashrc) has been minimal.

Need to implement better system/management of dotfiles.

Most of the current bashrc is the default from Pop!_OS.
